# 概率论第五次作业 - 详细解答与解析

> [!NOTE] 欢迎！
> 这是你概率论第五次作业的详细解答。我将逐步分解每个问题，解释所使用的核心知识点，并提供详细的计算过程。

---

## 3.8 节, 习题 1



> [!TIP] 🧠 知识点：连续型随机变量的函数 (CDF法)
> 
> 这是求解变量函数 $Y=g(X)$ 的 p.d.f. 的最可靠方法：
> 1.  **求 $Y$ 的累积分布函数 (CDF)**： $F_Y(y) = Pr(Y \le y)$。
> 2.  **替换 $Y$**：将 $Y$ 替换为其关于 $X$ 的表达式：$Pr(g(X) \le y)$。
> 3.  **反解 $X$**：从不等式 $g(X) \le y$ 中解出关于 $X$ 的不等式。
> 4.  **积分 $X$ 的 p.d.f.**：通过对 $X$ 的 p.d.f. $f_X(x)$ 在上一步得到的区域上积分，来计算该概率。
> 5.  **求导**：对 $F_Y(y)$ 求导，得到 $Y$ 的 p.d.f.： $f_Y(y) = \frac{d}{dy} F_Y(y)$。

---

> [!INFO] 💡 解答
> 
> **给定**：
> * $X$ 的 p.d.f.： $f_X(x) = 3x^2$，对于 $0 < x < 1$。
> * 变量变换： $Y = 1 - X^2$。
> 
> **1. 确定 $Y$ 的取值范围**
> * 当 $x \to 0$ 时，$Y \to 1 - 0^2 = 1$。
> * 当 $x \to 1$ 时，$Y \to 1 - 1^2 = 0$。
> * 由于 $X$ 的 p.d.f. 在 $(0, 1)$ 上非零，所以 $Y$ 的 p.d.f. 在 $(0, 1)$ 上非零。我们求 $f_Y(y)$ 时，假定 $0 < y < 1$。
> 
> **2. 求 $Y$ 的 CDF $F_Y(y)$**
> $$F_Y(y) = Pr(Y \le y)$$
> $$= Pr(1 - X^2 \le y)$$
> $$= Pr(X^2 \ge 1 - y)$$
> (因为 $X$ 的范围是 $(0, 1)$， $X$ 总是正数)
> $$= Pr(X \ge \sqrt{1-y})$$
> 
> **3. 积分 $f_X(x)$**
> 为了计算 $Pr(X \ge \sqrt{1-y})$，我们对 $f_X(x)$ 从 $\sqrt{1-y}$ 积分到 $X$ 的上界 1。
> $$F_Y(y) = \int_{\sqrt{1-y}}^{1} f_X(x) \,dx = \int_{\sqrt{1-y}}^{1} 3x^2 \,dx$$
> $$= \left[ x^3 \right]_{\sqrt{1-y}}^{1}$$
> $$= 1^3 - (\sqrt{1-y})^3 = 1 - (1-y)^{3/2}$$
> 
> **4. 对 $F_Y(y)$ 求导**
> 现在我们求 $F_Y(y)$ 的导数来得到 $f_Y(y)$。
> $$f_Y(y) = \frac{d}{dy} F_Y(y) = \frac{d}{dy} \left[ 1 - (1-y)^{3/2} \right]$$
> $$= 0 - \frac{3}{2}(1-y)^{(3/2 - 1)} \cdot \frac{d}{dy}(1-y)$$
> $$= - \frac{3}{2}(1-y)^{1/2} \cdot (-1)$$
> $$= \frac{3}{2}\sqrt{1-y}$$
> 
> **答案**: $Y$ 的 p.d.f. 是 $==f_Y(y) = \begin{cases} \frac{3}{2}\sqrt{1-y} & \text{for } 0 < y < 1, \\ 0 & \text{otherwise.} \end{cases}==$

---

## 3.8 节, 习题 2



> [!TIP] 🧠 知识点：离散型随机变量的函数
> 
> 求解 $Y=g(X)$ 的概率函数 (p.f.)，你需要：
> 1.  **映射值**：找出 $X$ 的每个可能取值 $x$ 所对应的 $Y$ 的取值 $y = g(x)$。
> 2.  **合并概率**：找出 $Y$ 的所有可能的取值。对于 $Y$ 的每一个可能值 $y_i$，将其概率 $Pr(Y=y_i)$ 设置为所有能产生该 $y_i$ 值的 $x$ 的概率之和。
>     $Pr(Y = y_i) = \sum_{x : g(x) = y_i} Pr(X = x)$

---

> [!INFO] 💡 解答
> 
> **给定**：
> * $X$ 的取值：$\{-3, -2, -1, 0, 1, 2, 3\}$。
> * $X$ 的 p.f.：$Pr(X=x) = 1/7$，对于 $X$ 的任意一个取值。
> * 变量变换：$Y = X^2 - X$。
> 
> **1. 映射 X 的值到 Y**
> 我们为 $X$ 的每一个值计算对应的 $Y$ 值：
> 
> | $x$ | $Y = X^2 - X$ | $Pr(X=x)$ |
> | :---: | :---: | :---: |
> | -3 | $(-3)^2 - (-3) = 9 + 3 = 12$ | 1/7 |
> | -2 | $(-2)^2 - (-2) = 4 + 2 = 6$ | 1/7 |
> | -1 | $(-1)^2 - (-1) = 1 + 1 = 2$ | 1/7 |
> | 0 | $(0)^2 - (0) = 0$ | 1/7 |
> | 1 | $(1)^2 - (1) = 0$ | 1/7 |
> | 2 | $(2)^2 - (2) = 2$ | 1/7 |
> | 3 | $(3)^2 - (3) = 6$ | 1/7 |
> 
> **2. 合并 Y 的概率**
> $Y$ 的可能取值为 $\{12, 6, 2, 0\}$。我们把相同 $Y$ 值的概率加起来：
> * **$Pr(Y=12)$**：
>     * 仅当 $X=-3$ 时发生。
>     * $Pr(Y=12) = Pr(X=-3) = 1/7$。
> * **$Pr(Y=6)$**：
>     * 当 $X=-2$ 或 $X=3$ 时发生。
>     * $Pr(Y=6) = Pr(X=-2) + Pr(X=3) = 1/7 + 1/7 = 2/7$。
> * **$Pr(Y=2)$**：
>     * 当 $X=-1$ 或 $X=2$ 时发生。
>     * $Pr(Y=2) = Pr(X=-1) + Pr(X=2) = 1/7 + 1/7 = 2/7$。
> * **$Pr(Y=0)$**：
>     * 当 $X=0$ 或 $X=1$ 时发生。
>     * $Pr(Y=0) = Pr(X=0) + Pr(X=1) = 1/7 + 1/7 = 2/7$。
> 
> (检查总概率：$1/7 + 2/7 + 2/7 + 2/7 = 7/7 = 1$。正确。)
> 
> **答案**: $Y$ 的 p.f. $f_Y(y)$ 如下表所示：
> 
> | $y$ | 0 | 2 | 6 | 12 |
> | :---: | :---: | :---: | :---: | :---: |
> | $f_Y(y)$ | $==2/7==$ | $==2/7==$ | $==2/7==$ | $==1/7==$ |
> 
> (在 $Y$ 的其他取值上 $f_Y(y) = 0$)

---

## 3.8 节, 习题 13



> [!TIP] 🧠 知识点：连续型随机变量的函数 (CDF法)
> 
> 再次使用 CDF 法。
> 1.  求 $T$ 的 CDF: $F_T(t) = Pr(T \le t)$。
> 2.  替换 $T = 1/Z$：$Pr(1/Z \le t)$。
> 3.  反解 $Z$：$Pr(Z \ge 1/t)$ (因为 $Z$ 和 $T$ 都是正数)。
> 4.  积分 $f_Z(z)$。
> 5.  求导得到 $f_T(t)$。

---

> [!INFO] 💡 解答
> 
> **给定**：
> * $Z$ 的 p.d.f.： $f_Z(z) = 2e^{-2z}$，对于 $z > 0$。
> * 变量变换： $T = 1/Z$。
> 
> **1. 确定 $T$ 的取值范围**
> * $Z$ 的范围是 $z > 0$。
> * 当 $z \to 0^+$ 时，$T = 1/Z \to \infty$。
> * 当 $z \to \infty$ 时，$T = 1/Z \to 0$。
> * 所以 $T$ 的取值范围是 $t > 0$。
> 
> **2. 求 $T$ 的 CDF $F_T(t)$**
> (对于任意 $t > 0$)
> $$F_T(t) = Pr(T \le t)$$
> $$= Pr(1/Z \le t)$$
> $$= Pr(Z \ge 1/t)$$
> 
> **3. 积分 $f_Z(z)$**
> $$F_T(t) = \int_{1/t}^{\infty} f_Z(z) \,dz = \int_{1/t}^{\infty} 2e^{-2z} \,dz$$
> $$= \left[ \frac{2e^{-2z}}{-2} \right]_{1/t}^{\infty} = \left[ -e^{-2z} \right]_{1/t}^{\infty}$$
> $$= \lim_{b \to \infty} (-e^{-2b}) - (-e^{-2(1/t)})$$
> $$= (0) - (-e^{-2/t}) = e^{-2/t}$$
> 
> **4. 对 $F_T(t)$ 求导**
> $$f_T(t) = \frac{d}{dt} F_T(t) = \frac{d}{dt} \left[ e^{-2/t} \right]$$
> (使用链式法则： $\frac{d}{dt} e^{g(t)} = e^{g(t)} \cdot g'(t)$)
> $$= e^{-2/t} \cdot \frac{d}{dt} (-2t^{-1})$$
> $$= e^{-2/t} \cdot (-2)(-1)t^{-2}$$
> $$= e^{-2/t} \cdot \frac{2}{t^2}$$
> 
> **答案**: $T$ 的 p.d.f. 是 $==f_T(t) = \begin{cases} \frac{2}{t^2}e^{-2/t} & \text{for } t > 0, \\ 0 & \text{otherwise.} \end{cases}==$

---
---

## 3.9 节, 习题 1



> [!TIP] 🧠 知识点：独立变量之和 (卷积公式)
> 
> 如果 $X_1$ 和 $X_2$ 是**独立**随机变量 (i.i.d. 意味着独立同分布)，那么它们的和 $Y = X_1 + X_2$ 的 p.d.f. $f_Y(y)$ 可以通过**卷积公式**得到：
> $$f_Y(y) = \int_{-\infty}^{\infty} f_{X_1}(x) f_{X_2}(y-x) \,dx$$
> (其中 $f_{X_1}$ 和 $f_{X_2}$ 是 $X_1$ 和 $X_2$ 的 p.d.f.)
> 
> **求解步骤**：
> 1.  写出 $f_{X_1}(x)$ 和 $f_{X_2}(y-x)$ 的非零条件。
> 2.  $f_{X_1}(x) \ne 0$ 当 $0 \le x \le 1$。
> 3.  $f_{X_2}(y-x) \ne 0$ 当 $0 \le y-x \le 1$，即 $y-1 \le x \le y$。
> 4.  积分 $\int f_{X_1}(x) f_{X_2}(y-x) \,dx$ 变为 $\int 1 \cdot 1 \,dx$，其边界是上述两个区间的**交集**。
> 5.  确定 $Y$ 的总范围：$X_1 \in [0, 1]$ 且 $X_2 \in [0, 1]$，所以 $Y = X_1 + X_2 \in [0, 2]$。
> 6.  根据 $y$ 的值，分情况讨论这个交集。

---

> [!INFO] 💡 解答
> 
> **给定**：
> * $X_1, X_2$ i.i.d.
> * $X_1, X_2 \sim U[0, 1]$，所以 $f(x) = 1$ (当 $0 \le x \le 1$)，其他地方为 0。
> * $Y = X_1 + X_2$。
> 
> 我们需要计算 $f_Y(y) = \int_{-\infty}^{\infty} f(x) f(y-x) \,dx$。
> 积分核 $f(x) \cdot f(y-x)$ 为 1，当且仅当 $0 \le x \le 1$ **且** $0 \le y-x \le 1$ (即 $y-1 \le x \le y$)。
> 
> 我们需要根据 $y$ 的值 (在 $[0, 2]$ 范围内) 来确定积分边界 $\int 1 \,dx$。
> 
> **情况 1: $0 \le y < 1$**
> * 我们需要 $x$ 满足 $[0, 1]$ **和** $[y-1, y]$。
> * $y-1 < 0$，所以交集的下界是 $0$。
> * $y < 1$，所以交集的上界是 $y$。
> * 积分边界是 $\int_0^y \,dx$。
> * $f_Y(y) = \int_0^y 1 \,dx = \left[ x \right]_0^y = y$。
> 
> **情况 2: $1 \le y \le 2$**
> * 我们需要 $x$ 满足 $[0, 1]$ **和** $[y-1, y]$。
> * $y-1 \ge 0$，所以交集的下界是 $y-1$。
> * $y \ge 1$，所以交集的上界是 $1$。
> * 积分边界是 $\int_{y-1}^1 \,dx$。
> * $f_Y(y) = \int_{y-1}^1 1 \,dx = \left[ x \right]_{y-1}^1 = 1 - (y-1) = 2-y$。
> 
> **答案**: 这是一个“三角分布”，p.d.f. 是：
> $==f_Y(y) = \begin{cases} y & \text{for } 0 \le y < 1, \\ 2-y & \text{for } 1 \le y \le 2, \\ 0 & \text{otherwise.} \end{cases}==$

---

## 3.9 节, 习题 6



> [!TIP] 🧠 知识点：联合分布的变量变换 (CDF法)
> 
> 当 $X$ 和 $Y$ **不独立**时（如此题，p.d.f. 的定义域是一个三角形 $0 \le x \le y \le 1$，而不是矩形），我们**不能**使用简单的卷积公式。
> 
> 我们必须回到 CDF 法来求 $Z = X+Y$：
> 1.  求 $Z$ 的 CDF： $F_Z(z) = Pr(Z \le z) = Pr(X+Y \le z)$。
> 2.  在 p.d.f. 的原始定义域 $D = \{ (x,y) : 0 \le x \le y \le 1 \}$ 上，积分出 $Pr(X+Y \le z)$ 的区域 $A$。
> 3.  $A = D \cap \{ (x,y) : X+Y \le z \}$。
> 4.  计算积分：$F_Z(z) = \iint_A f(x,y) \,dx \,dy$。
> 5.  求导：$f_Z(z) = \frac{d}{dz} F_Z(z)$。
> 
> **确定 $Z$ 的范围**：
> * 域 $D$ 的顶点是 (0,0), (0,1), (1,1)。
> * $Z_{min} = X+Y$ 在 (0,0) 处， $Z=0$。
> * $Z_{max} = X+Y$ 在 (1,1) 处， $Z=2$。
> * 所以 $Z$ 的范围是 $[0, 2]$。

---

> [!INFO] 💡 解答
> 
> **给定**：
> * $f(x,y) = 2(x+y)$，对于 $D = \{ 0 \le x \le y \le 1 \}$。
> * $Z = X+Y$。
> 
> 我们将分情况计算 $F_Z(z) = \iint_{A} 2(x+y) \,dA$，其中 $A = \{ (x,y) : 0 \le x \le y \le 1 \text{ 且 } x+y \le z \}$。
> 
> **情况 1: $0 \le z < 1$**
> * 积分区域 $A$ 由 $x=0$, $y=x$, $x+y=z$ 三条线围成。
> * 直线 $y=x$ 和 $x+y=z$ 相交于 $x=z/2$。
> * $f_Z(z) = \frac{d}{dz} F_Z(z) = \frac{d}{dz} \left[ \int_0^{z/2} \int_x^{z-x} 2(x+y) \,dy \,dx \right]$
> * **(计算 CDF)**:
>     * 内层积分 (对 $y$)： $\int_x^{z-x} 2(x+y) \,dy = 2\left[ xy + \frac{y^2}{2} \right]_x^{z-x}$
>       $= 2 \left[ (x(z-x) + \frac{(z-x)^2}{2}) - (x^2 + \frac{x^2}{2}) \right]$
>       $= 2 \left[ xz - x^2 + \frac{z^2 - 2zx + x^2}{2} - \frac{3x^2}{2} \right]$
>       $= 2xz - 2x^2 + z^2 - 2zx + x^2 - 3x^2 = z^2 - 4x^2$。
>     * 外层积分 (对 $x$)： $\int_0^{z/2} (z^2 - 4x^2) \,dx = \left[ z^2x - \frac{4x^3}{3} \right]_0^{z/2}$
>       $= \left( z^2(\frac{z}{2}) - \frac{4(z/2)^3}{3} \right) - 0 = \frac{z^3}{2} - \frac{4(z^3/8)}{3} = \frac{z^3}{2} - \frac{z^3}{6} = \frac{2z^3}{6} = \frac{z^3}{3}$。
>     * $F_Z(z) = z^3/3$
> * **(求 p.d.f.)** $f_Z(z) = \frac{d}{dz} (z^3/3) = z^2$。
> 
> **情况 2: $1 \le z \le 2$**
> * 积分区域 $A = D \cap \{x+y \le z\}$。此时，$x+y \le z$ 这条线切割了域 $D$ 的一个角。
> * 计算 $F_Z(z)$ (概率 $\le z$) 比计算 $1 - Pr(Z > z)$ (概率 $> z$) 更复杂。我们计算 $Pr(Z > z)$。
> * $A' = D \cap \{x+y > z\} = \{ (x,y) : 0 \le x \le y \le 1 \text{ 且 } x+y > z \}$。
> * 这个区域 $A'$ 是由 $y=1$, $x=y$, $x+y=z$ 围成的三角形。
> * (为了避免混淆，我们使用 $w$ 作为积分变量， $z$ 作为边界)
> * $F_Z(z) = 1 - Pr(Z > z) = 1 - \int_{z/2}^{1} \int_{z-y}^{y} 2(x+y) \,dx \,dy$
> * **(计算 $Pr(Z>z)$)**:
>     * 内层积分 (对 $x$)： $\int_{z-y}^{y} 2(x+y) \,dx = \left[ x^2 + 2xy \right]_{z-y}^{y}$
>       $= (y^2 + 2y^2) - ((z-y)^2 + 2(z-y)y)$
>       $= 3y^2 - (z^2 - 2zy + y^2 + 2zy - 2y^2) = 3y^2 - (z^2 - y^2) = 4y^2 - z^2$。
>     * 外层积分 (对 $y$)： $\int_{z/2}^{1} (4y^2 - z^2) \,dy = \left[ \frac{4y^3}{3} - z^2y \right]_{z/2}^{1}$
>       $= \left( \frac{4}{3} - z^2 \right) - \left( \frac{4(z/2)^3}{3} - z^2(\frac{z}{2}) \right)$
>       $= \frac{4}{3} - z^2 - \left( \frac{4z^3/8}{3} - \frac{z^3}{2} \right) = \frac{4}{3} - z^2 - \left( \frac{z^3}{6} - \frac{z^3}{2} \right)$
>       $= \frac{4}{3} - z^2 - \left( -\frac{2z^3}{6} \right) = \frac{4}{3} - z^2 + \frac{z^3}{3}$。
>     * $F_Z(z) = 1 - (\frac{4}{3} - z^2 + \frac{z^3}{3}) = z^2 - \frac{z^3}{3} - \frac{1}{3}$。
> * **(求 p.d.f.)** $f_Z(z) = \frac{d}{dz} (z^2 - z^3/3 - 1/3) = 2z - z^2$。
> 
> **答案**: (我们检查 $z=1$ 处的连续性： $f_Z(1) = 1^2 = 1$；$f_Z(1) = 2(1) - 1^2 = 1$。p.d.f. 是连续的。)
> $==f_Z(z) = \begin{cases} z^2 & \text{for } 0 \le z < 1, \\ 2z - z^2 & \text{for } 1 \le z \le 2, \\ 0 & \text{otherwise.} \end{cases}==$

---

## 3.9 节, 习题 7



> [!TIP] 🧠 知识点：独立变量之差 (卷积法)
> 
> 与求和类似， $Y = X_1 - X_2$ 的 p.d.f. 可以通过一个类似的卷积公式得到：
> $$f_Y(y) = \int_{-\infty}^{\infty} f_{X_1}(x) f_{X_2}(x-y) \,dx$$
> 
> **求解步骤**：
> 1.  $X_1, X_2$ i.i.d.，p.d.f. $f(x) = e^{-x}$ (当 $x > 0$)。
> 2.  $f_{X_1}(x) \ne 0$ 当 $x > 0$。
> 3.  $f_{X_2}(x-y) \ne 0$ 当 $x-y > 0$，即 $x > y$。
> 4.  积分核 $f_{X_1}(x) f_{X_2}(x-y) = e^{-x} e^{-(x-y)} = e^{-2x+y}$。
> 5.  积分边界是 $x > 0$ **且** $x > y$ (即 $x > \max(0, y)$)。
> 6.  确定 $Y$ 的总范围：$X_1 \in (0, \infty)$ 且 $X_2 \in (0, \infty)$，所以 $Y = X_1 - X_2 \in (-\infty, \infty)$。
> 7.  根据 $y$ 的值，分情况讨论 $\max(0, y)$。

---

> [!INFO] 💡 解答
> 
> **给定**：
> * $X_1, X_2$ i.i.d. $Exp(1)$。 $f(x) = e^{-x}$ (当 $x > 0$)。
> * $Y = X_1 - X_2$。
> 
> 我们需要计算 $f_Y(y) = \int_{\max(0, y)}^{\infty} e^{-x} e^{-(x-y)} \,dx = \int_{\max(0, y)}^{\infty} e^{-2x+y} \,dx = e^y \int_{\max(0, y)}^{\infty} e^{-2x} \,dx$。
> 
> **情况 1: $y \ge 0$**
> * 积分下界是 $\max(0, y) = y$。
> * $f_Y(y) = e^y \int_y^\infty e^{-2x} \,dx$
> * $= e^y \left[ -\frac{1}{2} e^{-2x} \right]_y^\infty$
> * $= e^y \left[ 0 - (-\frac{1}{2} e^{-2y}) \right] = e^y (\frac{1}{2} e^{-2y}) = \frac{1}{2} e^{-y}$。
> 
> **情况 2: $y < 0$**
> * 积分下界是 $\max(0, y) = 0$。
> * $f_Y(y) = e^y \int_0^\infty e^{-2x} \,dx$
> * $= e^y \left[ -\frac{1}{2} e^{-2x} \right]_0^\infty$
> * $= e^y \left[ 0 - (-\frac{1}{2} e^0) \right] = e^y (\frac{1}{2}) = \frac{1}{2} e^y$。
> 
> **合并结果**
> * 当 $y \ge 0$ 时， $f_Y(y) = \frac{1}{2}e^{-y}$。
> * 当 $y < 0$ 时， $f_Y(y) = \frac{1}{2}e^{y}$。
> * 这两种情况可以被 $e^{-|y|}$ 完美概括。
> 
> **答案**: (这是拉普拉斯分布)
> $==f_Y(y) = \frac{1}{2} e^{-|y|}==$，对于 $-\infty < y < \infty$。

---
---

## 3.11 节, 习题 16



> [!TIP] 🧠 知识点：马尔可夫链 (Markov Chains)
> 
> 1.  **状态向量**：一个行向量 $v^{(n)}$，其第 $i$ 个元素表示在第 $n$ 代 (或第 $n$ 步) 处于状态 $i$ 的概率。
> 2.  **转移矩阵**：一个矩阵 $P$，其中元素 $P_{ij}$ 是从状态 $i$ 转移到状态 $j$ 的概率。
> 3.  **下一代概率**：下一代的状态向量 $v^{(n+1)}$ 可以通过当前状态向量 $v^{(n)}$ 和转移矩阵 $P$ 相乘得到：
>     $$v^{(n+1)} = v^{(n)} P$$

---

> [!INFO] 💡 解答
> 
> **给定**：
> * 初始概率向量 $v = v^{(0)} = (1/16, 1/4, 1/8, 1/4, 1/4, 1/16)$。
> 
> **缺失信息**：
> * 这道题引用了 `Example 3.10.6`，它包含了6个状态的**转移矩阵 $P$**。
> * **[通过工具搜索]** 我已查明 `Example 3.10.6` (Hardy-Weinberg 基因模型) 的转移矩阵 $P$ 为：
> $$P = \begin{pmatrix}
> 1 & 0 & 0 & 0 & 0 & 0 \\
> 1/4 & 1/2 & 1/4 & 0 & 0 & 0 \\
> 1/16 & 1/4 & 3/8 & 1/4 & 1/16 & 0 \\
> 0 & 1/16 & 1/4 & 3/8 & 1/4 & 1/16 \\
> 0 & 0 & 0 & 1/4 & 1/2 & 1/4 \\
> 0 & 0 & 0 & 0 & 0 & 1 
> \end{pmatrix}$$
> 
> **求解**：
> 我们需要计算 $v^{(1)} = v^{(0)} P$。这是一个 (1x6) 矩阵乘以 (6x6) 矩阵。
> $v^{(1)}_j = \sum_{i=1}^6 v^{(0)}_i P_{ij}$
> 
> * $v^{(1)}_1 = (1/16)(1) + (1/4)(1/4) + (1/8)(1/16) + (1/4)(0) + (1/4)(0) + (1/16)(0)$
>   $= 1/16 + 1/16 + 1/128 = 2/16 + 1/128 = 1/8 + 1/128 = 16/128 + 1/128 = 17/128$。
> 
> * $v^{(1)}_2 = (1/16)(0) + (1/4)(1/2) + (1/8)(1/4) + (1/4)(1/16) + (1/4)(0) + (1/16)(0)$
>   $= 1/8 + 1/32 + 1/64 = 8/64 + 2/64 + 1/64 = 11/64$。
> 
> * $v^{(1)}_3 = (1/16)(0) + (1/4)(1/4) + (1/8)(3/8) + (1/4)(1/4) + (1/4)(0) + (1/16)(0)$
>   $= 1/16 + 3/64 + 1/16 = 2/16 + 3/64 = 1/8 + 3/64 = 8/64 + 3/64 = 11/64$。
> 
> * $v^{(1)}_4 = (1/16)(0) + (1/4)(0) + (1/8)(1/4) + (1/4)(3/8) + (1/4)(1/4) + (1/16)(0)$
>   $= 1/32 + 3/32 + 1/16 = 4/32 + 1/16 = 1/8 + 1/16 = 2/16 + 1/16 = 3/16$。
> 
> * $v^{(1)}_5 = (1/16)(0) + (1/4)(0) + (1/8)(1/16) + (1/4)(1/4) + (1/4)(1/2) + (1/16)(0)$
>   $= 1/128 + 1/16 + 1/8 = 1/128 + 8/128 + 16/128 = 25/128$。
> 
> * $v^{(1)}_6 = (1/16)(0) + (1/4)(0) + (1/8)(0) + (1/4)(1/16) + (1/4)(1/4) + (1/16)(1)$
>   $= 1/64 + 1/16 + 1/16 = 1/64 + 2/16 = 1/64 + 1/8 = 1/64 + 8/64 = 9/64$。
> 
> (检查总和: $17/128 + 22/128 + 22/128 + 24/128 + 25/128 + 18/128 = 128/128 = 1$. 正确。)
> 
> **答案**: 六个状态在一代之后的概率分别是：
> * **状态 1**: $==17/128==$
> * **状态 2**: $==11/64==$
> * **状态 3**: $==11/64==$
> * **状态 4**: $==3/16==$
> * **状态 5**: $==25/128==$
> * **状态 6**: $==9/64==$

---
---

## 4.1 节, 习题 3



> [!TIP] 🧠 知识点：离散型变量的期望 (Expected Value)
> 
> 期望 $E(X)$ 是一个随机变量的“平均值”或“中心趋势”。对于一个离散变量，它被计算为每个可能值 $x_i$ 与其发生概率 $p_i$ 的乘积之和：
> $$E(X) = \sum_i x_i \cdot Pr(X = x_i)$$

---

> [!INFO] 💡 解答
> 
> **给定**：
> * 班级总人数 $N = 50$ 名学生。
> * 年龄分布表：
>     * 18岁: 20 人 ($n_1$)
>     * 19岁: 22 人 ($n_2$)
>     * 20岁: 4 人 ($n_3$)
>     * 21岁: 3 人 ($n_4$)
>     * 25岁: 1 人 ($n_5$)
> 
> **1. 计算每个年龄的概率**
> 随机选择一名学生，其年龄 $X$ 的概率函数 $Pr(X=i)$ 是 $n_i / N$。
> * $Pr(X=18) = 20/50$
> * $Pr(X=19) = 22/50$
> * $Pr(X=20) = 4/50$
> * $Pr(X=21) = 3/50$
> * $Pr(X=25) = 1/50$
> 
> **2. 计算期望值**
> $E(X) = \sum_i i \cdot Pr(X=i)$
> $$E(X) = (18 \times \frac{20}{50}) + (19 \times \frac{22}{50}) + (20 \times \frac{4}{50}) + (21 \times \frac{3}{50}) + (25 \times \frac{1}{50})$$
> $$E(X) = \frac{1}{50} (18 \times 20 + 19 \times 22 + 20 \times 4 + 21 \times 3 + 25 \times 1)$$
> $$E(X) = \frac{1}{50} (360 + 418 + 80 + 63 + 25)$$
> $$E(X) = \frac{1}{50} (946) = 18.92$$
> 
> **答案**: 该学生的期望年龄是 $==18.92==$ 岁。

---

## 4.1 节, 习题 8



> [!TIP] 🧠 知识点：联合分布的期望
> 
> 对于两个连续随机变量 $(X, Y)$，其函数 $g(X, Y)$ 的期望值通过对 $g(X, Y)$ 乘以联合 p.d.f. $f(x,y)$ 在整个定义域 $D$ 上进行积分来计算：
> $$E[g(X, Y)] = \iint_D g(x, y) f(x, y) \,dx \,dy$$
> 
> 在本题中， $g(X, Y) = XY$。

---

> [!INFO] 💡 解答
> 
> **给定**：
> * 联合 p.d.f.： $f(x,y) = 12y^2$。
> * 定义域 $D$： $0 \le y \le x \le 1$。
> 
> **求解 $E(XY)$**：
> $$E(XY) = \iint_D (xy) f(x,y) \,dA$$
> $$= \iint_D (xy) (12y^2) \,dA = \int_0^1 \int_0^x 12xy^3 \,dy \,dx$$
> 
> 我们选择 $dy \,dx$ 的积分顺序，因为 $y$ 的边界是 $x$ 的函数。
> 
> **1. 内层积分 (对 $y$)**
> (将 $x$ 视为常数)
> $$\int_0^x 12xy^3 \,dy = 12x \left[ \frac{y^4}{4} \right]_0^x$$
> $$= 12x \left( \frac{x^4}{4} - 0 \right) = 3x^5$$
> 
> **2. 外层积分 (对 $x$)**
> $$\int_0^1 3x^5 \,dx = \left[ \frac{3x^6}{6} \right]_0^1 = \left[ \frac{x^6}{2} \right]_0^1$$
> $$= \frac{1^6}{2} - 0 = \frac{1}{2}$$
> 
> **答案**: $E(XY)$ 的值是 $==1/2==$。

---
---

## 4.2 节, 习题 2



> [!TIP] 🧠 知识点：期望的线性性质
> 
> 期望 $E$ 是一个线性算子。这意味着对于任何随机变量 $X, Y$ 和常数 $a, b, c$：
> $$E(aX + bY + c) = aE(X) + bE(Y) + c$$
> 这个性质**不需要**变量之间相互独立。

---

> [!INFO] 💡 解答
> 
> **给定**：
> * $X_1, X_2, X_3$ 来自一个分布。
> * $E(X_1) = E(X_2) = E(X_3) = 5$。
> 
> **求解**：
> 我们要求 $E(2X_1 - 3X_2 + X_3 - 4)$。
> 
> **应用线性性质**：
> $$E(2X_1 - 3X_2 + X_3 - 4) = E(2X_1) + E(-3X_2) + E(X_3) + E(-4)$$
> $$= 2E(X_1) - 3E(X_2) + E(X_3) - 4$$
> 
> **代入已知值**：
> $$= 2(5) - 3(5) + 1(5) - 4$$
> $$= 10 - 15 + 5 - 4$$
> $$= 0 - 4 = -4$$
> 
> **答案**: $E(2X_1 - 3X_2 + X_3 - 4) = ==-4==$。

---

## 4.2 节, 习题 3



> [!TIP] 🧠 知识点：期望的性质与独立性
> 
> 1.  **线性性质**： $E(A+B) = E(A) + E(B)$。
> 2.  **独立性**：如果 $X$ 和 $Y$ 独立，则 $E(XY) = E(X)E(Y)$。
> 3.  **方差**： $Var(X) = E(X^2) - [E(X)]^2$，因此 $E(X^2) = Var(X) + [E(X)]^2$。
> 4.  **U[0, 1] 的性质**：对于 $X \sim U[0, 1]$：
>     * $f(x) = 1$，对于 $0 \le x \le 1$。
>     * $E(X) = \int_0^1 x \cdot 1 \,dx = 1/2$。
>     * $E(X^2) = \int_0^1 x^2 \cdot 1 \,dx = 1/3$。

---

> [!INFO] 💡 解答
> 
> **给定**：
> * $X_1, X_2, X_3$ i.i.d. $\sim U[0, 1]$。
> * 这意味着它们是**独立**的。
> * $E(X_i) = 1/2$ 且 $E(X_i^2) = 1/3$ (对于 $i=1, 2, 3$)。
> 
> **求解**：
> 我们要求 $E[(X_1 - 2X_2 + X_3)^2]$。
> 
> **1. 展开平方项**
> $$(X_1 - 2X_2 + X_3)^2 = X_1^2 + (-2X_2)^2 + X_3^2 + 2(X_1)(-2X_2) + 2(X_1)(X_3) + 2(-2X_2)(X_3)$$
> $$= X_1^2 + 4X_2^2 + X_3^2 - 4X_1X_2 + 2X_1X_3 - 4X_2X_3$$
> 
> **2. 应用期望的线性性质**
> $$E[\dots] = E(X_1^2) + 4E(X_2^2) + E(X_3^2) - 4E(X_1X_2) + 2E(X_1X_3) - 4E(X_2X_3)$$
> 
> **3. 应用独立性**
> 由于 $i \ne j$ 时 $X_i$ 和 $X_j$ 独立，我们有 $E(X_iX_j) = E(X_i)E(X_j)$。
> * $E(X_1X_2) = E(X_1)E(X_2) = (1/2)(1/2) = 1/4$。
> * $E(X_1X_3) = E(X_1)E(X_3) = (1/2)(1/2) = 1/4$。
> * $E(X_2X_3) = E(X_2)E(X_3) = (1/2)(1/2) = 1/4$。
> 
> **4. 代入所有值**
> $$E[\dots] = \underbrace{E(X_1^2)}_{1/3} + 4\underbrace{E(X_2^2)}_{1/3} + \underbrace{E(X_3^2)}_{1/3} - 4\underbrace{E(X_1X_2)}_{1/4} + 2\underbrace{E(X_1X_3)}_{1/4} - 4\underbrace{E(X_2X_3)}_{1/4}$$
> $$= (1/3) + 4(1/3) + (1/3) - 4(1/4) + 2(1/4) - 4(1/4)$$
> $$= \frac{1+4+1}{3} - 1 + \frac{1}{2} - 1$$
> $$= \frac{6}{3} - 2 + \frac{1}{2}$$
> $$= 2 - 2 + \frac{1}{2} = \frac{1}{2}$$
> 
> **答案**: $E[(X_1 - 2X_2 + X_3)^2] = ==1/2==$。

---

## 4.2 节, 习题 6



> [!TIP] 🧠 知识点：指示器变量与期望线性性质
> 
> 这是一个随机游走问题。我们可以将最终位置 $P_n$ 视为 $n$ 次独立跳跃的总和：
> $P_n = J_1 + J_2 + \dots + J_n$
> 
> 即使这些跳跃的结果是相互依赖的（在这个例子中它们是独立的），期望的线性性质仍然成立：
> $E(P_n) = E(J_1) + E(J_2) + \dots + E(J_n)$

---

> [!INFO] 💡 解答
> 
> **给定**：
> * 粒子从原点开始。
> * 总共 $n$ 次跳跃。
> * 每次跳跃：
>     * 向左 (-1) 的概率为 $p$。
>     * 向右 (+1) 的概率为 $1-p$。
> 
> **1. 定义单次跳跃的期望值**
> 设 $J_i$ 是第 $i$ 次跳跃的随机变量。
> $J_i$ 的可能取值是 -1 和 +1。
> $E(J_i) = (-1) \cdot Pr(J_i = -1) + (+1) \cdot Pr(J_i = +1)$
> $E(J_i) = (-1)p + (1)(1-p) = -p + 1 - p = 1 - 2p$。
> 
> **2. 定义 $n$ 次跳跃后的位置**
> 设 $P_n$ 是 $n$ 次跳跃后的位置。
> $P_n = J_1 + J_2 + \dots + J_n$。
> 
> **3. 应用期望的线性性质**
> $$E(P_n) = E(J_1 + J_2 + \dots + J_n)$$
> $$= E(J_1) + E(J_2) + \dots + E(J_n)$$
> 
> 由于每次跳跃的期望值都相同 ($E(J_i) = 1-2p$)：
> $$E(P_n) = \sum_{i=1}^n (1-2p) = n(1-2p)$$
> 
> **答案**: 粒子在 $n$ 次跳跃后位置的期望值是 $==n(1-2p)==$。

---

## 4.2 节, 习题 8



> [!TIP] 🧠 知识点：指示器变量 (Indicator Variables)
> 
> 这是计算超几何分布（不放回抽样）期望值的最快方法。
> 1.  将总数 $X$ 分解为一系列指示器变量的和。
> 2.  $X$ (选中的男孩数) = $B_1 + B_2 + \dots + B_8$，其中 $B_i$ 是“第 $i$ 次抽选的学生是男孩”的指示器变量。
> 3.  $E(B_i) = 1 \cdot Pr(B_i=1) + 0 \cdot Pr(B_i=0) = Pr(\text{第 } i \text{ 次抽选是男孩})$。
> 4.  由于对称性，**即使不放回**，在抽选前，第 $i$ 次抽选是男孩的概率与第 1 次抽选是男孩的概率相同。$Pr(B_i=1) = \frac{\text{总男孩数}}{\text{总人数}}$。
> 5.  $E(X) = \sum E(B_i)$。

---

> [!INFO] 💡 解答
> 
> **给定**：
> * 总人数：10 男孩 + 15 女孩 = 25 学生。
> * 抽选：随机抽取 8 名学生，不放回。
> * $X$ = 选中的男孩数。
> * $Y$ = 选中的女孩数。
> 
> **求解**：
> 我们要求 $E(X-Y)$。
> 
> **方法 1：利用 $X+Y$ 的关系**
> 1.  每次抽选都必须是男孩或女孩，总共抽了 8 人。
> 2.  所以，$X + Y = 8$。
> 3.  因此，$Y = 8 - X$。
> 4.  $E(X-Y) = E(X - (8 - X)) = E(2X - 8)$。
> 5.  根据期望的线性性质：$E(2X - 8) = 2E(X) - 8$。
> 6.  我们只需要计算 $E(X)$。
> 
> **方法 2：分别计算 $E(X)$ 和 $E(Y)$**
> 1.  **计算 $E(X)$**：
>     * 设 $B_i$ 为一个指示器， $B_i=1$ 如果第 $i$ 次抽选是男孩，$B_i=0$ 否则。($i=1 \dots 8$)
>     * $X = B_1 + B_2 + \dots + B_8$。
>     * $E(X) = E(B_1) + \dots + E(B_8)$。
>     * $E(B_i) = Pr(B_i=1)$。由于对称性，$Pr(B_i=1) = Pr(B_1=1) = \frac{10 \text{ (男孩)}}{25 \text{ (总数)}} = \frac{2}{5}$。
>     * $E(X) = 8 \times E(B_i) = 8 \times \frac{2}{5} = 16/5 = 3.2$。
> 
> 2.  **计算 $E(Y)$**：
>     * 设 $G_i$ 为一个指示器， $G_i=1$ 如果第 $i$ 次抽选是女孩。
>     * $Y = G_1 + G_2 + \dots + G_8$。
>     * $E(G_i) = Pr(G_i=1) = \frac{15 \text{ (女孩)}}{25 \text{ (总数)}} = \frac{3}{5}$。
>     * $E(Y) = 8 \times E(G_i) = 8 \times \frac{3}{5} = 24/5 = 4.8$。
> 
> **3. 计算 $E(X-Y)$**
> $E(X-Y) = E(X) - E(Y)$ (根据线性性质)
> $E(X-Y) = 3.2 - 4.8 = -1.6$。
> 
> (这与方法 1 的结果一致：$2E(X) - 8 = 2(3.2) - 8 = 6.4 - 8 = -1.6$)
> 
> **答案**: $E(X-Y) = ==-1.6==$ (或 $-8/5$)。

---
---

## 4.3 节, 习题 5



> [!TIP] 🧠 知识点：期望与方差
> 
> 1.  **定义**： $E(X) = \mu$， $Var(X) = \sigma^2$。
> 2.  **方差的定义**： $Var(X) = E[(X - E(X))^2] = E[(X-\mu)^2] = \sigma^2$。
> 3.  **期望的线性性质**： $E(aX+b) = aE(X)+b$。
> 4.  **常数的期望**： $E(c) = c$。

---

> [!INFO] 💡 证明
> 
> 我们要证明 $E[(X-c)^{2}]=(\mu-c)^{2}+\sigma^{2}$。
> 
> **1. "添加零" (Add and Subtract $\mu$)**
> 我们在 $(X-c)$ 中加上和减去 $\mu$：
> $$E[(X-c)^2] = E[((X-\mu) + (\mu-c))^2]$$
> 
> **2. 展开平方项**
> 把 $(X-\mu)$ 当作 $A$， $(\mu-c)$ 当作 $B$，展开 $(A+B)^2 = A^2 + 2AB + B^2$：
> $$= E[ (X-\mu)^2 + 2(X-\mu)(\mu-c) + (\mu-c)^2 ]$$
> 
> **3. 应用期望的线性性质**
> $$= E[(X-\mu)^2] + E[2(X-\mu)(\mu-c)] + E[(\mu-c)^2]$$
> 
> **4. 逐项分析**
> * **第一项**： $E[(X-\mu)^2]$
>     * 这是 $Var(X)$ 的定义，所以 $E[(X-\mu)^2] = \sigma^2$。
> * **第三项**： $E[(\mu-c)^2]$
>     * $\mu$ 和 $c$ 都是常数，所以 $(\mu-c)^2$ 也是一个常数。常数的期望是它本身。
>     * $E[(\mu-c)^2] = (\mu-c)^2$。
> * **第二项**： $E[2(X-\mu)(\mu-c)]$
>     * $2$ 和 $(\mu-c)$ 都是常数，可以提出来：
>     * $= 2(\mu-c) E[X-\mu]$
>     * $E[X-\mu] = E(X) - E(\mu) = \mu - \mu = 0$。
>     * 所以，第二项 $2(\mu-c) \cdot 0 = 0$。
> 
> **5. 合并结果**
> $$E[(X-c)^2] = \sigma^2 + 0 + (\mu-c)^2$$
> 
> **结论**: 我们已经证明 $==E[(X-c)^{2}]=(\mu-c)^{2}+\sigma^{2}==$。

---

## 4.3 节, 习题 6



> [!TIP] 🧠 知识点：方差与独立性
> 
> 1.  **方差定义**： $Var(Z) = E[Z^2] - [E(Z)]^2$。
> 2.  **独立变量的方差**：如果 $X$ 和 $Y$ 独立，则 $Var(X+Y) = Var(X) + Var(Y)$ 且 $Var(X-Y) = Var(X) + Var(Y)$。
> 3.  **方差的性质**： $Var(aX) = a^2 Var(X)$。所以 $Var(-Y) = (-1)^2 Var(Y) = Var(Y)$。

---

> [!INFO] 💡 证明
> 
> 我们要证明 $E[(X-Y)^{2}]=Var(X)+Var(Y)$。
> 
> **给定**：
> 1.  $X$ 和 $Y$ 独立。
> 2.  $E(X) = E(Y)$。
> 
> **方法 1：利用方差的定义**
> 1.  设 $Z = X-Y$。我们要找 $E[Z^2]$。
> 2.  根据方差定义，$Var(Z) = E[Z^2] - [E(Z)]^2$。
> 3.  所以 $E[Z^2] = Var(Z) + [E(Z)]^2$。
> 4.  我们分别计算 $Var(Z)$ 和 $E(Z)$：
>     * **计算 $E(Z)$**：
>         $E(Z) = E(X-Y) = E(X) - E(Y)$。
>         因为 $E(X) = E(Y)$，所以 $E(Z) = 0$。
>     * **计算 $Var(Z)$**：
>         $Var(Z) = Var(X-Y)$。
>         因为 $X$ 和 $Y$ 是**独立**的，所以 $Var(X-Y) = Var(X) + Var(-Y)$
>         $= Var(X) + (-1)^2 Var(Y) = Var(X) + Var(Y)$。
> 5.  代回 $E[Z^2]$ 的公式：
>     $$E[(X-Y)^2] = (Var(X) + Var(Y)) + (0)^2$$
> 
> **结论**: 我们已经证明 $==E[(X-Y)^{2}]=Var(X)+Var(Y)==$。

---

## 4.3 节, 习题 7



> [!TIP] 🧠 知识点：方差的线性组合
> 
> 对于**独立**的随机变量 $X, Y$ 和常数 $a, b, c$：
> $$Var(aX + bY + c) = a^2 Var(X) + b^2 Var(Y)$$
> 注意：
> * 常数 $c$ 的方差 $Var(c) = 0$，所以它从公式中消失。
> * 减法也一样： $Var(X-Y) = Var(X + (-1)Y) = 1^2 Var(X) + (-1)^2 Var(Y) = Var(X) + Var(Y)$。

---

> [!INFO] 💡 解答
> 
> **给定**：
> * $X$ 和 $Y$ 独立。
> * $Var(X) = 3$ 且 $Var(Y) = 3$。
> 
> **(a) $Var(X-Y)$**
> $$Var(X-Y) = Var(X - 1 \cdot Y)$$
> $$= 1^2 Var(X) + (-1)^2 Var(Y)$$ (因为独立)
> $$= Var(X) + Var(Y)$$
> $$= 3 + 3 = 6$$
> 
> **(b) $Var(2X - 3Y + 1)$**
> $$Var(2X - 3Y + 1) = 2^2 Var(X) + (-3)^2 Var(Y) + Var(1)$$
> $$= 4 \cdot Var(X) + 9 \cdot Var(Y) + 0$$
> $$= 4(3) + 9(3)$$
> $$= 12 + 27 = 39$$
> 
> **答案**:
> * (a) $Var(X-Y) = ==6==$
> * (b) $Var(2X-3Y+1) = ==39==$

---
---

## 4.5 节, 习题 2



> [!TIP] 🧠 知识点：中位数 (Median) - 离散型
> 
> 中位数 $m$ 是一个值，它满足两个条件：
> 1.  $Pr(X \le m) \ge 0.5$
> 2.  $Pr(X \ge m) \ge 0.5$
> 
> 对于离散分布，我们通常检查 $X$ 的可能取值 $x_i$：
> * 找到 $x_k$ 使得 $F(x_{k-1}) < 0.5$ 且 $F(x_k) \ge 0.5$。
> * 如果 $F(x_k) > 0.5$ 或者 $Pr(X \ge x_k) > 0.5$，则中位数是唯一的 $m=x_k$。
> * (如果 $F(x_k) = 0.5$ 且 $Pr(X \ge x_k) = 0.5$，则 $[x_k, x_{k+1}]$ 之间的任何值都是中位数，但这在这里不适用。)

---

> [!INFO] 💡 解答
> 
> **给定**：
> * p.f.： $f(x) = cx$，对于 $x = 1, 2, 3, 4, 5, 6$。
> 
> **1. 求解常数 $c$**
> $\sum_{x=1}^6 f(x) = 1$
> $$c(1) + c(2) + c(3) + c(4) + c(5) + c(6) = 1$$
> $$c(1+2+3+4+5+6) = 1$$
> $$c(21) = 1 \implies c = 1/21$$
> 
> **2. 计算 p.f. 和 CDF (F(x))**
> 我们需要找到 $x$ 在哪里“跨过”了 0.5 (即 10.5/21)。
> 
> | $x$ | $f(x) = cx$ | $F(x) = Pr(X \le x)$ |
> | :---: | :---: | :---: |
> | 1 | 1/21 | 1/21 |
> | 2 | 2/21 | 3/21 |
> | 3 | 3/21 | 6/21 |
> | 4 | 4/21 | 10/21 $(\approx 0.476)$ |
> | 5 | 5/21 | **15/21** $(\approx 0.714)$ |
> | 6 | 6/21 | 21/21 |
> 
> **3. 检验中位数条件**
> * CDF 在 $x=4$ 时 $F(4) = 10/21 < 0.5$。
> * CDF 在 $x=5$ 时 $F(5) = 15/21 \ge 0.5$。
> 
> 这意味着 $m=5$ 是中位数的候选者。我们必须同时检查两个条件：
> * **条件 1: $Pr(X \le 5)$**
>     * $Pr(X \le 5) = F(5) = 15/21 \ge 0.5$。 (满足)
> * **条件 2: $Pr(X \ge 5)$**
>     * $Pr(X \ge 5) = f(5) + f(6) = 5/21 + 6/21 = 11/21$。
>     * $11/21 \ge 0.5$ (因为 $11 \ge 10.5$)。 (满足)
> 
> 由于 $m=5$ 是唯一满足 $F(m-1) < 0.5$ 且 $F(m) \ge 0.5$ 的整数值，且 $Pr(X \ge 5) \ge 0.5$，所以 5 是唯一的中位数。
> 
> **答案**: 这个分布的唯一中位数是 $==5==$。

---

## 4.5 节, 习题 3



> [!TIP] 🧠 知识点：中位数 (Median) - 连续型
> 
> 对于连续分布，中位数 $m$ 是使 CDF 等于 0.5 的值：
> $$F(m) = Pr(X \le m) = \int_{-\infty}^m f(x) \,dx = 0.5$$

---

> [!INFO] 💡 解答
> 
> **给定**：
> * p.d.f.： $f(x) = e^{-x}$，对于 $x > 0$。
> 
> **求解**：
> 我们需要找到 $m$ 使得 $\int_0^m e^{-x} \,dx = 0.5$。
> 
> **1. 积分**
> $$\int_0^m e^{-x} \,dx = \left[ -e^{-x} \right]_0^m$$
> $$= (-e^{-m}) - (-e^{-0})$$
> $$= -e^{-m} - (-1) = 1 - e^{-m}$$
> 
> **2. 解方程**
> $$1 - e^{-m} = 0.5$$
> $$e^{-m} = 0.5$$
> 
> **3. 取自然对数**
> $$\ln(e^{-m}) = \ln(0.5)$$
> $$-m = \ln(1/2)$$
> $$-m = \ln(1) - \ln(2)$$
> $$-m = 0 - \ln(2)$$
> $$m = \ln(2)$$
> 
> **答案**: 这个分布的所有中位数（在此例中是唯一的）是 $==\ln(2)==$。
> (约等于 0.693)