![89f24fec-dfcd-46d4-a049-2163a191b752.png](https://cc-407-1376569927.cos.ap-guangzhou.myqcloud.com/cc-407-1376569927/images-obsidian/202511161937500.png)

# 📘 大学物理 Homework 17 解析 (Chapter 29)

> [!abstract] **笔记简介**
> 本笔记包含 Chapter 29 的 Problem 11, 21, 和 27(a) 的详细解析。
> * **涉及核心概念：** 平面电磁波、坡印廷矢量 (Poynting Vector)、能量传输、安培-麦克斯韦定律。
> * **参考资料：** 基于用户提供的教科书 Example 29-1 图片资料。

---

## 1️⃣ Problem 11: Plane EM Wave Analysis

> [!question] **题目描述**
> The electric field of a plane EM wave is given by $E_x = E_0 \cos(kz + \omega t)$, $E_y = E_z = 0$. Determine:
> * (a) The magnitude and direction of $\mathbf{B}$.
> * (b) The direction of propagation.

### 🧠 核心知识点 (Key Concepts)

1.  **传播方向 (Direction of Propagation):**
    在波函数 $\cos(kz \pm \omega t)$ 中：
    * $kz - \omega t$ 表示沿 $+z$ 方向传播。
    * $kz + \omega t$ 表示沿 $\color{#d63031}{-z}$ **方向传播**。
2.  **场强关系 (Magnitude Relation):**
    在真空中，$E$ 和 $B$ 的幅值关系为：
    $$B_0 = \frac{E_0}{c}$$
3.  **方向关系 (Vector Relation):**
    电磁波传播方向 $\mathbf{v}$ (或坡印廷矢量 $\mathbf{S}$) 与 $\mathbf{E}$、$\mathbf{B}$ 遵循右手定则：
    $$\hat{\mathbf{k}}_{prop} = \hat{\mathbf{E}} \times \hat{\mathbf{B}}$$
    即：$\mathbf{E} \times \mathbf{B}$ 的方向必须指向波的传播方向。

### 📝 详细解答 (Solution)

#### (b) 确定传播方向 (先做b问有助于确定a问的方向)

观察给定的相位项 $(kz + \omega t)$。
由于 $z$ 和 $t$ 的系数符号**相同**（都是正号），这意味着波也是向 $z$ 轴负方向移动以保持相位恒定。

> [!success] **结论 (b)**
> 波的传播方向是 **沿 $-z$ 方向** (Negative z-direction)。

#### (a) 确定 $\mathbf{B}$ 的大小和方向

* **大小 (Magnitude):**
    磁场幅值为 $B_0 = E_0 / c$。
    波的形式与电场一致，因此 $\mathbf{B}$ 的标量部分是 $B_x = \frac{E_0}{c} \cos(kz + \omega t)$。

* **方向 (Direction):**
    已知：
    * $\mathbf{E}$ 的方向是 $+x$ ($\hat{\mathbf{i}}$)。
    * 传播方向是 $-z$ ($-\hat{\mathbf{k}}$)。
    
    根据右手定则 $\hat{\mathbf{E}} \times \hat{\mathbf{B}} = \text{传播方向}$：
    $$\hat{\mathbf{i}} \times \hat{\mathbf{B}} = -\hat{\mathbf{k}}$$
    
    回顾向量叉乘循环 ($\mathbf{i} \rightarrow \mathbf{j} \rightarrow \mathbf{k} \rightarrow \mathbf{i}$):
    我们知道 $\hat{\mathbf{i}} \times \hat{\mathbf{j}} = \hat{\mathbf{k}}$，那么 $\hat{\mathbf{i}} \times (-\hat{\mathbf{j}}) = -\hat{\mathbf{k}}$。
    
    因此，$\mathbf{B}$ 的方向必须是 **$-y$ 方向** ($-\hat{\mathbf{j}}$)。

> [!success] **结论 (a)**
> $\mathbf{B}$ 的矢量表达式为：
> $$\mathbf{B} = -\frac{E_0}{c} \cos(kz + \omega t) \hat{\mathbf{j}}$$
> * **Magnitude:** $B = \frac{E_0}{c} \cos(kz + \omega t)$
> * **Direction:** $-y$ axis.

---

## 2️⃣ Problem 21: Energy Transport

> [!question] **题目描述**
> How much energy is transported across a $1.00 \, \text{cm}^2$ area per hour by an EM wave whose $E$ field has an rms strength of $28.6 \, \text{mV/m}$?

### 🧠 核心知识点 (Key Concepts)

1.  **平均强度 (Intensity / Average Poynting Vector):**
    电磁波的平均能流密度 $I$ (即 $\bar{S}$) 与电场有效值 $E_{\text{rms}}$ 的关系：
    $$I = \epsilon_0 c E_{\text{rms}}^2$$
    * $\epsilon_0 \approx 8.85 \times 10^{-12} \, \text{C}^2/\text{N}\cdot\text{m}^2$
    * $c \approx 3.00 \times 10^8 \, \text{m/s}$
2.  **能量计算 (Total Energy):**
    $$U = I \times A \times \Delta t$$

### 📝 详细解答 (Solution)

**1. 单位换算 (Unit Conversion):**
> [!warning] **注意单位统一**
> * $E_{\text{rms}} = 28.6 \, \text{mV/m} = \color{#e17055}{28.6 \times 10^{-3} \, \text{V/m}}$
> * $A = 1.00 \, \text{cm}^2 = \color{#e17055}{1.00 \times 10^{-4} \, \text{m}^2}$
> * $\Delta t = 1 \, \text{hour} = \color{#e17055}{3600 \, \text{s}}$

**2. 计算平均强度 $I$:**
$$
\begin{aligned}
I &= \epsilon_0 c E_{\text{rms}}^2 \\
&= (8.85 \times 10^{-12}) (3.00 \times 10^8) (28.6 \times 10^{-3})^2 \\
&\approx 2.655 \times 10^{-3} \times (8.18 \times 10^{-4}) \\
&\approx \mathbf{2.17 \times 10^{-6} \, \text{W/m}^2}
\end{aligned}
$$

**3. 计算总能量 $U$:**
$$
\begin{aligned}
U &= I \cdot A \cdot \Delta t \\
&= (2.17 \times 10^{-6} \, \text{J/s}\cdot\text{m}^2) \times (1.00 \times 10^{-4} \, \text{m}^2) \times (3600 \, \text{s}) \\
&= 2.17 \times 10^{-10} \times 3600 \\
&\approx 7.81 \times 10^{-7} \, \text{J}
\end{aligned}
$$

> [!success] **最终答案**
> The energy transported is approximately **$7.8 \times 10^{-7} \, \text{J}$**.

---

## 3️⃣ Problem 27(a): Poynting Vector in Capacitor

> [!question] **题目描述**
> Show that the Poynting vector $\mathbf{S}$ points **radially inward** toward the center of a circular parallel-plate capacitor when it is being charged as in Example 29-1.

### 🧠 核心知识点 (Key Concepts)

1.  **坡印廷矢量 (Poynting Vector):**
    $$\mathbf{S} = \frac{1}{\mu_0} \mathbf{E} \times \mathbf{B}$$
    它代表电磁能量流动的方向。
2.  **充电电容中的场 (Fields in Charging Capacitor):**
    根据你提供的图片 (Example 29-1 和 Fig 29-4)：
    * **$\mathbf{E}$ 场:** 均匀分布在极板间，方向从正极板指向负极板（随时间增强）。
    * **$\mathbf{B}$ 场:** 由变化的电场（位移电流）感应产生。根据安培-麦克斯韦定律，磁感线是同心圆。

### 📝 详细解答 (Solution)
![824e30d0-78a2-4767-b297-4cc0fb24a3b2.png](https://cc-407-1376569927.cos.ap-guangzhou.myqcloud.com/cc-407-1376569927/images-obsidian/202511161938719.png)
![b00241ef-15fb-4c86-b00b-33327be1a7de.png](https://cc-407-1376569927.cos.ap-guangzhou.myqcloud.com/cc-407-1376569927/images-obsidian/202511161938000.png)

![5b3ac1dc-5b4d-43b1-b6d0-6f3335cd9ac5.png](https://cc-407-1376569927.cos.ap-guangzhou.myqcloud.com/cc-407-1376569927/images-obsidian/202511161938989.png)
![7d82df67-d6de-474f-b367-14847dc92b81.png](https://cc-407-1376569927.cos.ap-guangzhou.myqcloud.com/cc-407-1376569927/images-obsidian/202511161938563.png)

我们结合你提供的 textbook 图片进行几何分析。

**Step 1: 确定 $\mathbf{E}$ 的方向**
在平行板电容器内部，假设左板带正电，右板带负电（或者参考 Fig 29-4，$\mathbf{E}$ 是垂直纸面向外）。
* 让我们设定圆柱坐标系。
* 假设 $\mathbf{E}$ 沿着 $z$ 轴方向（垂直于极板）。
* 即 $\mathbf{E} = E \, \hat{\mathbf{k}}$ (假设指向上方/或纸面外)。

**Step 2: 确定 $\mathbf{B}$ 的方向**
根据 Example 29-1 和 Ampere's Law ($\oint \mathbf{B} \cdot d\mathbf{l} = \mu_0 \epsilon_0 \frac{d\Phi_E}{dt}$):
* 因为电容正在**充电 (Charging)**，$\frac{dE}{dt} > 0$。
* 根据右手螺旋定则（类似于电流产生磁场，这里是位移电流）：
    * 大拇指指向 $\frac{d\mathbf{E}}{dt}$ 的方向（即 $\hat{\mathbf{k}}$）。
    * 四指弯曲的方向即为 $\mathbf{B}$ 的方向。
* 因此，$\mathbf{B}$ 场是**逆时针方向**的同心圆（如果从 $\mathbf{E}$ 指向的一侧看）。
* 在圆柱坐标系中，$\mathbf{B}$ 沿着切向 $\hat{\boldsymbol{\phi}}$。
* 即 $\mathbf{B} = B \, \hat{\boldsymbol{\phi}}$。

**Step 3: 计算 $\mathbf{S}$ 的方向**
计算叉乘 $\mathbf{S} = \frac{1}{\mu_0} \mathbf{E} \times \mathbf{B}$:

$$
\mathbf{S} \propto (\hat{\mathbf{k}}) \times (\hat{\boldsymbol{\phi}})
$$

回顾圆柱坐标系的基向量关系 ($\hat{\mathbf{r}}, \hat{\boldsymbol{\phi}}, \hat{\mathbf{k}}$)：
* $\hat{\mathbf{r}} \times \hat{\boldsymbol{\phi}} = \hat{\mathbf{k}}$
* $\hat{\boldsymbol{\phi}} \times \hat{\mathbf{k}} = \hat{\mathbf{r}}$
* $\color{#e84393}{\hat{\mathbf{k}} \times \hat{\boldsymbol{\phi}} = -\hat{\mathbf{r}}}$

> [!info] **物理意义解读**
> 结果 $-\hat{\mathbf{r}}$ 表示方向是 **Radially Inward (沿半径向内)** 指向电容器中心轴线。

**Step 4: 结合 Fig 29-4 (Visual Proof)**
参考你上传的图片 `image_91fa4e.jpg` (Figure 29-4):
* **E (out):** 电场垂直纸面向外。
* **B (circles):** 磁场是逆时针的圆。
* **在圆的最右侧点:** $\mathbf{E}$ 向外 ($\odot$), $\mathbf{B}$ 向上 ($\uparrow$).
    * 右手定则: 手掌向外，手指向上卷，大拇指指向 **左 (Left)** $\leftarrow$。即指向圆心。
* **在圆的最上侧点:** $\mathbf{E}$ 向外 ($\odot$), $\mathbf{B}$ 向左 ($\leftarrow$).
    * 右手定则: 手掌向外，手指向左卷，大拇指指向 **下 (Down)** $\downarrow$。即指向圆心。

> [!success] **证明结论**
> 无论在电容器边缘的哪一点，$\mathbf{S} = \frac{1}{\mu_0} \mathbf{E} \times \mathbf{B}$ 的方向总是指向圆心。
> 这表明：**能量正在从外部空间通过侧面流入电容器，转化为极板间的电场能。**
> (Energy is flowing into the capacitor from the sides to be stored in the electric field).