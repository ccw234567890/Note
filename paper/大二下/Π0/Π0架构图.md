
# π₀ 架构图

## 整体架构概览

```mermaid
graph TB
    subgraph Input["输入层 (Observation $$o_t$$)"]
        I1["图像 $$I_{t1}$$"]
        I2["图像 $$I_{t2}$$"]
        I3["图像 $$I_{t3}$$"]
        L["语言指令 $$l_t$$"]
        Q["本体感知状态 $$q_t$$ (18维)"]
        N["噪声动作 $$A_t^\\tau$$ (H=50)"]
    end

    subgraph Encoder["编码器"]
        ViT["SigLIP ViT<br/>图像编码器<br/>(权重共享)"]
        LP["线性投影层<br/>$$q_t \\to \\mathbb{R}^w$$"]
        MLP["MLP嵌入层<br/>$$a_{t'}^\\tau + \\phi(\\tau) \\to \\mathbb{R}^w$$"]
    end

    subgraph Transformer["Transformer 骨干网络 (3.3B参数)"]
        subgraph VLM_Backbone["VLM 骨干 (PaliGemma 2B)<br/>width=2048, depth=18<br/>multi-query attention"]
            direction TB
            VLM_L1["Layer 1"]
            VLM_L2["Layer 2"]
            VLM_L3["..."]
            VLM_L18["Layer 18"]
        end

        subgraph Action_Expert["动作专家 (300M)<br/>width=1024, mlp_dim=4096"]
            direction TB
            AE_L1["Layer 1"]
            AE_L2["Layer 2"]
            AE_L3["..."]
            AE_LN["Layer N"]
        end

        subgraph Attention_Mask["注意力掩码<br/>(Blockwise Causal)"]
            Block1["Block 1: [图像, 语言]<br/>双向注意力<br/>← 不能看块2，3"]
            Block2["Block 2: [$$q_t$$]<br/>双向注意力<br/>← 不能看块3"]
            Block3["Block 3: [$$A_t^\\tau$$]<br/>双向注意力<br/>→ 可看块1，2"]
        end
    end

    subgraph Output["输出层"]
        Proj["线性投影层<br/>$$\\mathbb{R}^w \\to \\mathbb{R}^{18}$$"]
        VF["向量场 $$v_\\theta(A_t^\\tau, o_t)$$"]
    end

    subgraph FlowMatching["Flow Matching 生成过程"]
        Init["初始化<br/>$$A_t^0 \\sim \\mathcal{N}(0, I)$$"]
        Step1["Step 1<br/>$$A_t^{0.1} = A_t^0 + 0.1 \\cdot v_\\theta$$"]
        Step2["Step 2<br/>$$A_t^{0.2} = A_t^{0.1} + 0.1 \\cdot v_\\theta$$"]
        StepN["... ×10步 ..."]
        Final["最终动作块<br/>$$A_t = [a_t, a_{t+1}, ..., a_{t+49}]$$"]
    end

    subgraph RobotControl["机器人控制"]
        Robot["机器人执行器<br/>(50 Hz / 20 Hz)"]
        Action["动作指令<br/>关节角度/末端位姿"]
    end

    I1 & I2 & I3 --> ViT
    ViT --> Transformer
    L --> Transformer
    Q --> LP --> Transformer
    N --> MLP --> Transformer

    Transformer --> Proj --> VF
    VF --> FlowMatching

    Init --> Step1 --> Step2 --> StepN --> Final
    Final --> Action --> Robot
```

---

## 数据流详解

```mermaid
flowchart LR
    subgraph Obs["观测 $$o_t$$"]
        direction LR
        IMG["多视角RGB图像<br/>2-3张"]
        TXT["语言指令<br/>token序列"]
        PROP["本体感知 $$q_t$$<br/>18维向量"]
    end

    subgraph Enc["编码阶段"]
        direction TB
        V["ViT编码器<br/>→图像patch嵌入"]
        L["PaliGemma<br/>→语言token嵌入"]
        P["线性投影<br/>→$$q_t$$嵌入"]
    end

    subgraph FM["流匹配阶段"]
        direction LR
        N1["噪声 $$A_t^0$$"]
        AE["动作专家<br/>+ VLM骨干<br/>(KV缓存)"]
        VF2["$$v_\\theta$$"]
        N2["$$A_t^{0.1}$$"]
    end

    subgraph Out["输出"]
        A["动作块 $$A_t$$<br/>H=50步"]
    end

    IMG --> V
    TXT --> L
    PROP --> P
    V & L & P --> AE
    N1 --> AE
    AE --> VF2 --> N2
    N2 -. "×10次迭代" .-> AE
    N2 --> A
```

---

## 注意力掩码结构

```mermaid
graph TD
    subgraph Mask["Blockwise Causal Attention Mask"]
        direction LR
        T1["图像令牌<br/>$$I_{t1}...I_{tn}$$"]
        T2["语言令牌<br/>$$l_t$$"]
        T3["状态令牌<br/>$$q_t$$"]
        T4["动作令牌<br/>$$a_t^\\tau...a_{t+49}^\\tau$$"]
    end

    T1 -->|"双向"| T1
    T1 -->|"双向"| T2
    T1 -->|"不能看"| T3
    T1 -->|"不能看"| T4

    T2 -->|"双向"| T1
    T2 -->|"双向"| T2
    T2 -->|"不能看"| T3
    T2 -->|"不能看"| T4

    T3 -->|"不能看"| T1
    T3 -->|"不能看"| T2
    T3 -->|"双向"| T3
    T3 -->|"不能看"| T4

    T4 -->|"可看"| T1
    T4 -->|"可看"| T2
    T4 -->|"可看"| T3
    T4 -->|"双向"| T4
```

---

## 训练流程

```mermaid
flowchart TB
    subgraph PretrainData["预训练数据混合"]
        OXE["OXE 开源数据集<br/>22种机器人<br/>2-10 Hz"]
        PI["π 自有数据集<br/>7种机器人配置<br/>68个任务<br/>~10,000小时"]
    end

    subgraph Pretrain["预训练阶段"]
        Init2["初始化<br/>VLM骨干 ← PaliGemma<br/>动作专家 ← 随机初始化"]
        Train["训练<br/>Flow Matching Loss<br/>$$\\mathcal{L}_\\tau(\\theta) = \\mathbb{E}\\|v_\\theta - u\\|^2$$"]
        Base["基础模型 π₀<br/>广泛能力<br/>基础熟练度"]
    end

    subgraph Posttrain["后训练阶段"]
        HighQ["高质量任务数据<br/>5-100+小时"]
        Finetune["微调<br/>Flow Matching Loss"]
        Expert["专用任务模型<br/>高灵巧度<br/>高效执行"]
    end

    PretrainData --> Pretrain
    Init2 --> Train --> Base
    HighQ --> Posttrain
    Base --> Posttrain
    Posttrain --> Finetune --> Expert

    subgraph Tasks["下游任务"]
        Laundry["叠衣服<br/>(5-20分钟)"]
        Bussing["清理餐桌"]
        Box["组装纸箱"]
        Eggs["装鸡蛋"]
    end

    Expert --> Tasks
```

---

## 关键参数汇总

| 组件 | 参数 | 说明 |
|------|------|------|
| VLM 骨干 | PaliGemma 2B | width=2048, depth=18, mlp_dim=16384, num_heads=18, num_kv_heads=1, head_dim=256 |
| 动作专家 | ~300M | width=1024, mlp_dim=4096 |
| 总参数量 | ~3.3B | 2B + 300M + ViT 编码器等 |
| 动作块长度 H | 50 | 一次预测50步动作 |
| 流匹配步数 | 10 | δ=0.1 欧拉积分 |
| 控制频率 | 50 Hz / 20 Hz | 灵巧任务50Hz，UR5e/Franka 20Hz |
| 推理延迟 | ~73ms (板载) / ~86ms (无线) | NVIDIA RTX 4090 |
| 状态/动作维度 | 18 | 零填充兼容不同机器人 |
| 图像数量 | 2-3 | 腕部相机 + 外部相机 |

---

Written by LLM-for-Zotero.
