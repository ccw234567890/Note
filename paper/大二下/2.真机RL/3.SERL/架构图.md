---
title: "SERL 系统架构图"
created: 2026-04-16
tags: [zotero, architecture, serl]
---

# SERL 系统架构图

## 一、SERL 整体系统架构

```mermaid
graph TB
    subgraph RL策略层_10Hz
        A[策略网络 Actor pi_theta]
        B[价值网络 Critic Q_phi]
        C[回放缓冲区 Replay Buffer]
        D[演示数据 Demonstrations]
    end

    subgraph 中层_奖励与重置模块
        E[奖励函数模块]
        F[前向-后向重置控制器]
    end

    subgraph 低层_阻抗控制器层_1KHz
        G[阻抗控制器]
        H[参考限幅机制]
        I[底层电机驱动]
    end

    subgraph 真实世界环境
        J[Franka Panda 机器人]
        K[腕部相机 x2]
        L[物理交互对象]
    end

    D -- 对称采样 --> C
    C -- 采样batch --> A
    C -- 采样batch --> B
    A -- 6D delta pose --> G
    G -- F = kp e + kd edot + Fff + Fcor --> H
    H -- 限幅 e le Delta --> I
    I -- 执行动作 --> J
    J -- 物理交互 --> L
    K -- 图像观测 --> A
    J -- 本体感知信息 --> A
    L -- 接触力反馈 --> G
    A -- 动作数据 --> C
    J -- 状态转移 s到s' --> C
    E -- 奖励信号 r --> C
    F -- 自动重置 --> J
```

---

## 二、核心RL算法：RLPD (基于SAC)

```mermaid
graph LR
    subgraph RLPD训练循环
        T1[环境交互 1个时间步] -- s a r s' --> T2[回放缓冲区]
        T2 -- 对称采样 50pct演示+50pct在线 --> T3[多次梯度更新 UTD大于1]
        T3 -- 更新Q网络 --> T4[Critic损失 MSBE]
        T3 -- 更新pi网络 --> T5[Actor损失 最大熵]
        T5 -- 新策略 --> T1
    end

    subgraph 损失函数
        L1[L_Q = E Q_phi - r + gamma E Qbar_phi s' a' 平方]
        L2[L_pi = -E E Q_phi s a + alpha H pi_theta s]
    end

    T4 --> L1
    T5 --> L2
```

---

## 三、奖励函数模块

```mermaid
graph TB
    subgraph 三种奖励方式
        R1[手工指定奖励 基于末端位置]
        R2[二分类器奖励 r s = log p e s]
        R3[VICE对抗式奖励 策略vs分类器 GAN式训练]
    end

    subgraph VICE训练流程
        V1[初始分类器 正样本：成功演示] --> V2[RL策略探索]
        V2 -- 策略访问的状态 --> V3[加入分类器训练集 标记为负样本]
        V3 --> V4[更新分类器]
        V4 -- 新奖励信号 --> V2
    end

    R2 -- 需要负样本 --> V1
    R3 --> V2
```

---

## 四、阻抗控制器与参考限幅

```mermaid
graph TB
    subgraph 控制层级
        RL[RL策略输出 6D delta pose 10Hz] -- p_ref --> SM[平滑处理]
        SM -- 平滑后的目标 --> CL[参考限幅 e le Delta]
        CL -- 限幅后的目标 --> IC[阻抗控制器 1KHz]
        IC -- F = kp e + kd edot + Fff + Fcor --> JT[关节空间力矩 J^T F + 零空间力矩]
        JT -- 执行 --> ROBOT[机器人]
    end

    subgraph 限幅效果
        FS[自由空间 M Delta ge a_max 快速移动不受限]
        CT[接触时 力被约束为 kp Delta + 2kd Delta f]
    end

    CL --> FS
    CL --> CT
```

---

## 五、相对观测与动作坐标系

```mermaid
graph LR
    subgraph 坐标变换
        S[基坐标系 s] -- T_b0 --> B0[回合初始末端坐标系 b_i_0]
        S -- T_bt --> BT[当前末端坐标系 b_i_t]
        B0 -- T_inv_b0 --> REL[相对变换 T_b0_bt = T_inv_b0 乘 T_bt]
        BT -- 策略输入 --> REL
        REL -- 相对位姿 --> POLICY[策略网络]
        POLICY -- 6D twist 在b_i_t中表达 --> ADJ[伴随映射 chi' = Ad chi]
        ADJ -- 6D twist chi' 在s中表达 --> CONTROLLER[控制器]
    end

    subgraph 伴随矩阵
        ADJ_MAT[Ad = R 0 分号 p_x R R]
    end

    ADJ --> ADJ_MAT
```

---

## 六、前向-后向重置训练

```mermaid
graph TB
    subgraph 双策略并行训练
        FW[前向策略 Forward Policy 执行任务]
        BW[后向策略 Backward Policy 重置环境]
    end

    subgraph 训练循环
        FW -- 完成任务 --> BW
        BW -- 重置到初始状态 --> FW
    end

    subgraph 各策略独立组件
        FW_Q[前向Q函数]
        FW_R[前向奖励函数]
        BW_Q[后向Q函数]
        BW_R[后向奖励函数]
    end

    FW --> FW_Q
    FW --> FW_R
    BW --> BW_Q
    BW --> BW_R
```

---

## 七、三进程并行架构

```mermaid
graph LR
    subgraph 进程1_Actor节点
        ACT[策略推理 选择动作 固定控制频率]
    end

    subgraph 进程2_Learner节点
        LRN[训练更新 高UTD比率 多次梯度更新]
    end

    subgraph 进程3_机器人环境
        ENV[执行动作 采集数据 返回观测]
    end

    ACT -- 动作指令 --> ENV
    ENV -- 状态/奖励数据 --> LRN
    LRN -- 更新策略参数 --> ACT
```

---

## 八、三个实验任务概览

```mermaid
graph TB
    subgraph PCB插装
        P1[演示数 20]
        P2[训练时间 20分钟]
        P3[成功率 100pct]
        P4[奖励 手工指定]
        P5[精度 亚毫米级]
    end

    subgraph 线缆布线
        C1[演示数 20]
        C2[训练时间 31分钟]
        C3[成功率 100pct]
        C4[奖励 二分类器]
        C5[难点 可变形物体]
    end

    subgraph 物体搬运
        O1[演示数 20]
        O2[训练时间 约105分钟 前向+后向]
        O3[成功率 100pct]
        O4[奖励 二分类器+VICE]
        O5[特点 自动重置]
    end
```

---

## 九、关键公式汇总

| 组件       | 公式                                                                                                                                              | 说明          |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| MDP目标    | $$J(\pi) = \mathbb{E}\left[\sum_{t=0}^{\infty} \gamma^t r(s_t, a_t)\right]$$                                                                    | 最大化期望累积回报   |
| Critic损失 | $$\mathcal{L}_Q(\phi) = \mathbb{E}_{s,a,s'}\left[(Q_\phi(s,a) - (r(s,a) + \gamma\mathbb{E}_{a'\sim\pi_\theta}[\bar{Q}_\phi(s',a')]))^2\right]$$ | 均方贝尔曼误差     |
| Actor损失  | $$\mathcal{L}_\pi(\theta) = -\mathbb{E}_s\left[\mathbb{E}_{a\sim\pi_\theta(a)}[Q_\phi(s,a)] + \alpha\mathcal{H}(\pi_\theta(\cdots))\right]$$    | 最大熵策略优化     |
| 分类器奖励    | $$r(s) = \log p(e\|s)$$                                                                                                                         | 基于成功概率的对数奖励 |
| 阻抗控制     | $$F = k_p \cdot e + k_d \cdot \dot{e} + F_{ff} + F_{cor}$$                                                                                      | 弹簧-阻尼系统     |
| 参考限幅     | $$\|e\| \leq \Delta$$                                                                                                                           | 接触力约束       |
| 齐次变换     | $$T_{ab} = \begin{bmatrix} R_{ab} & p_{ab} \\ 0_{1\times3} & 1 \end{bmatrix}$$                                                                  | 相对坐标系变换     |
| 伴随映射     | $$[Ad] = \begin{bmatrix} R & 0_{3\times3} \\ [p]_\times R & R \end{bmatrix}$$                                                                   | 动作坐标系转换     |

---

Written by LLM-for-Zotero.
