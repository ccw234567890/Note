
# $\pi_{0.7}$ 架构图

## 总体架构

```mermaid
graph TB
    subgraph Input["输入层"]
        direction TB
        OBS["多视角观测<br/>前视 + 2×腕部 + 后视<br/>每视角最多6帧历史<br/>448×448 像素"]
        PROP["本体感知状态<br/>关节位置/末端执行器位姿<br/>线性投影嵌入"]
        LANG["语言指令<br/>任务描述 l_t<br/>子任务指令 l̂_t"]
        META["片段元数据<br/>速度(离散化500步区间)<br/>质量(1-5分)<br/>错误标记(true/false)"]
        CTRL["控制模式<br/>joint / end-effector"]
        SG["子目标图像<br/>多视角<br/>最多3张<br/>448×448 像素"]
    end

    subgraph VLM["VLM 骨干网络 (4B)"]
        direction TB
        VE["视觉编码器 (400M)<br/>MEM 风格视频历史编码器<br/>时间+空间压缩<br/>输出固定数量 token"]
        LLM["Gemma3 4B 语言模型<br/>FAST token 交叉熵损失<br/>知识隔离(KI)训练<br/>动作专家梯度不回流"]
       
    end

    subgraph ActionExpert["动作专家 (860M)"]
        direction TB
        FM["流匹配(Flow Matching)<br/>连续动作生成模型<br/>自适应 RMSNorm<br/>注入时间步信息"]
        RTC["实时动作分块(RTC)<br/>训练时模拟0-12步延迟<br/>50Hz → 最大240ms延迟"]
        CHUNK["动作块输出<br/>固定50步动作块<br/>执行 Ĥ ∈ {15,25} 步<br/>5步去噪生成"]
    end

    subgraph WorldModel["世界模型 (BAGEL 14B)"]
        direction TB
        WM_INPUT["输入<br/>当前观测 o_t<br/>子任务指令 l̂_t<br/>元数据 m"]
        WM_GEN["图像生成<br/>流匹配损失 L_CFM<br/>网络规模视频预训练<br/>人类视频+网络数据"]
        WM_OUTPUT["输出<br/>多视角子目标图像 g*<br/>每 ∆=4秒 刷新<br/>语义变化时刷新"]
    end

    subgraph HighLevel["高层策略"]
        direction TB
        HL_INPUT["输入<br/>机器人观测<br/>任务描述<br/>历史子任务指令"]
        HL_OUTPUT["输出<br/>子任务指令 l̂_t<br/>支持人类语言教练<br/>可训练为自主策略"]
    end

    subgraph Output["输出层"]
        direction TB
        ACT["动作执行<br/>PD 控制器<br/>数值逆运动学<br/>关节/末端执行器命令"]
    end

    Input --> VLM
    Input --> ActionExpert
    OBS --> VE
    VE --> LLM
    PROP --> LLM
    LANG --> LLM
    META --> LLM
    CTRL --> LLM
    SG --> VE
    LLM --> ActionExpert
    ActionExpert --> ACT
    HighLevel --> LANG
    WorldModel --> SG
```

## 训练数据组成

```mermaid
graph LR
    subgraph RobotData["机器人数据"]
        DEMO["演示数据<br/>高质量人类遥操作"]
        AUTO["自主数据<br/>策略评估 rollout<br/>含失败和次优轨迹"]
        INTERV["人类干预数据<br/>策略执行中干预记录"]
        OPEN["开源机器人数据集<br/>跨本体数据"]
        RL_DATA["π₀*.6 RL 专家数据<br/>RL 训练中收集的 rollout"]
    end

    subgraph NonRobotData["非机器人数据"]
        EGO["自我中心人类视频<br/>第一人称操作视频"]
        WEB["网络数据<br/>目标定位/属性预测<br/>VQA/文本预测/视频描述"]
    end

    subgraph Training["训练流程"]
        TRAIN["π₀.₇ 训练<br/>监督学习<br/>流匹配 + 交叉熵"]
    end

    RobotData --> Training
    NonRobotData --> Training
```

## 推理流程

```mermaid
sequenceDiagram
    participant H as 高层策略/人类
    participant WM as 世界模型 (BAGEL 14B)
    participant VLA as π₀.₇ VLA 模型
    participant R as 机器人

    H->>VLA: 任务指令 l, 元数据 m, 控制模式 c
    H->>WM: 子任务指令 l̂
    WM->>WM: 生成子目标图像 g* ~ p_ψ(g* | o₀, l̂, m)
    WM->>VLA: 子目标图像 g*
    Note over VLA: 组合上下文 C = {l, l̂, g*, m, c}

    loop 每 Ĥ 步执行
        VLA->>VLA: 生成动作块 a_{t:t+H} ~ π_θ(a | o_{t-T:t}, C)
        VLA->>R: 执行 Ĥ 步动作
        R->>VLA: 新观测 o_t
    end

    Note over WM,VLA: 异步推理
    alt l̂ 改变 或 ∆=4秒 超时
        H->>WM: 新子任务指令 l̂'
        WM->>WM: 刷新子目标图像 g*'
        WM->>VLA: 更新上下文 C
    end
```

## 注意力掩码机制

```mermaid
graph TB
    subgraph Mask["注意力掩码结构 (Block-Causal)"]
        direction TB
        OBS_TOKENS["观测 token<br/>双向自注意力"]
        SG_TOKENS["子目标图像 token<br/>双向自注意力<br/>可额外关注观测"]
        TEXT_TOKENS["文本 token<br/>因果注意力<br/>只能关注前面"]
        ACT_TOKENS["动作 token (动作专家)<br/>双向自注意力<br/>可关注 VLM 全部激活"]
    end

    OBS_TOKENS -->|双向| OBS_TOKENS
    OBS_TOKENS -->|双向| SG_TOKENS
    SG_TOKENS -->|双向| SG_TOKENS
    SG_TOKENS -->|单向| OBS_TOKENS
    TEXT_TOKENS -->|因果| TEXT_TOKENS
    TEXT_TOKENS -->|因果| OBS_TOKENS
    TEXT_TOKENS -->|因果| SG_TOKENS
    ACT_TOKENS -->|双向| ACT_TOKENS
    ACT_TOKENS -->|单向| VLM_ALL["VLM 全部激活"]
```

## 机器人平台

```mermaid
graph TB
    subgraph Platforms["机器人平台"]
        MOBILE["移动双臂操作器<br/>2×6 DoF 臂<br/>1 DoF 夹爪<br/>1-2 DoF 升降<br/>3 DoF 全向底座<br/>50 Hz"]
        BIPI["静态双臂机器人 (BiPi)<br/>2×6 DoF 轻量臂<br/>1 DoF 夹爪<br/>50 Hz"]
        UR5E["双臂 UR5e<br/>2×6 DoF 臂<br/>Robotiq 夹爪<br/>20 Hz<br/>跨本体迁移目标"]
        SINGLE["单臂 6 DoF 系统<br/>6 DoF 臂<br/>1 DoF 夹爪<br/>语言泛化实验"]
    end

    subgraph Config["传感器配置"]
        FRONT["前视摄像头"]
        WRIST_L["左腕部摄像头"]
        WRIST_R["右腕部摄像头"]
        REAR["后视摄像头 (移动平台)"]
    end

    MOBILE --> FRONT
    MOBILE --> WRIST_L
    MOBILE --> WRIST_R
    MOBILE --> REAR
    BIPI --> FRONT
    BIPI --> WRIST_L
    BIPI --> WRIST_R
    UR5E --> FRONT
    UR5E --> WRIST_L
    UR5E --> WRIST_R
    SINGLE --> FRONT
    SINGLE --> WRIST_L
```

## 关键参数汇总

| 参数 | 值 |
|------|-----|
| VLM 骨干 | Gemma3 4B (含 400M 视觉编码器) |
| 动作专家 | 860M Transformer |
| 总参数量 | ~5B |
| 世界模型 | BAGEL 14B MoT |
| 输入图像分辨率 | 448×448 |
| 摄像头视角 | 最多 4 个 (前视+2×腕部+后视) |
| 历史帧数 | 每视角最多 6 帧 (步长 1秒) |
| 子目标图像 | 最多 3 张 (不含后视) |
| 动作块长度 | 50 步 |
| 执行步数 Ĥ | 15-25 步 |
| 机器人频率 | 50 Hz (UR5e: 20 Hz) |
| 子目标刷新间隔 | ∆=4 秒 |
| CFG 权重 β | {1.3, 1.7, 2.2} |
| 去噪步数 | 5 步 |
| 训练随机丢弃 | 子目标图像 25% 添加, 子任务指令 30%, 元数据 15%, 历史帧 30%, 后视 30% |

---

Written by LLM-for-Zotero.
