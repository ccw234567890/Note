---
title: "SAC 算法架构图"
created: 2026-05-10
tags: [zotero, sac, reinforcement-learning]
---

# SAC 算法架构图

## 1. 整体架构总览

```mermaid
graph TB
    subgraph 环境交互层
        Env[环境 Environment]
        State[状态 s]
        Reward[奖励 r]
    end

    subgraph 经验回放层
        RB[Replay Buffer D]
    end

    subgraph Actor网络
        Policy[策略网络 pi_phi]
        Noise[噪声 epsilon]
        Reparam[重参数化]
        Tanh[Tanh压缩]
    end

    subgraph Critic网络
        Q1[Q函数 Q_theta1]
        Q2[Q函数 Q_theta2]
        Min[取最小值]
        TargetQ1[目标网络 Q_theta_bar1]
        TargetQ2[目标网络 Q_theta_bar2]
        TargetMin[取最小值]
    end

    subgraph 温度参数自动调节
        Alpha[温度 alpha]
        AlphaLoss[J_alpha 损失]
    end

    subgraph 损失函数
        JQ[Critic Loss J_Q]
        JPi[Actor Loss J_pi]
    end

    Env -->|s| Policy
    Policy -->|pi_phi| Noise
    Noise --> Reparam
    Reparam --> Tanh
    Tanh -->|a| Env
    Env -->|r s_next| RB
    State --> RB

    RB -->|采样| Q1
    RB -->|采样| Q2
    RB -->|采样| Policy

    Q1 --> Min
    Q2 --> Min
    Min --> JPi
    Min --> JQ

    TargetQ1 --> TargetMin
    TargetQ2 --> TargetMin
    TargetMin -->|V s_next| JQ

    RB -->|采样| TargetQ1
    RB -->|采样| TargetQ2

    JQ -->|梯度| Q1
    JQ -->|梯度| Q2
    JPi -->|梯度| Policy

    AlphaLoss -->|梯度| Alpha
    Alpha -->|alpha| JPi
    Alpha -->|alpha| JQ

    Q1 -.->|EMA tau| TargetQ1
    Q2 -.->|EMA tau| TargetQ2

    style Env fill:#4CAF50,color:#fff
    style Policy fill:#2196F3,color:#fff
    style Q1 fill:#FF9800,color:#fff
    style Q2 fill:#FF9800,color:#fff
    style Alpha fill:#9C27B0,color:#fff
    style RB fill:#607D8B,color:#fff
```

## 2. 算法伪代码流程

```mermaid
flowchart TD
    Start([开始]) --> Init[初始化参数 theta1 theta2 phi]
    Init --> InitTarget[初始化目标网络]
    InitTarget --> InitRB[初始化空回放池 D]
    InitRB --> LoopStart{每个迭代}

    LoopStart --> EnvStep[环境交互步]
    EnvStep --> SampleAction[从策略采样动作 a]
    SampleAction --> StepEnv[执行动作 得到 s_next]
    StepEnv --> Store[存储 s a r s_next 到 D]
    Store --> CheckEnv{达到梯度步数}

    CheckEnv -->|否| EnvStep
    CheckEnv -->|是| GradStep[梯度更新步]

    GradStep --> UpdateQ[更新 Q 函数参数 theta_i]
    UpdateQ --> UpdatePolicy[更新策略参数 phi]
    UpdatePolicy --> UpdateAlpha[调整温度 alpha]
    UpdateAlpha --> UpdateTarget[更新目标网络]
    UpdateTarget --> CheckDone{是否收敛}

    CheckDone -->|否| LoopStart
    CheckDone -->|是| Output([输出优化参数])
```

## 3. Soft 策略评估（Critic 更新）

```mermaid
flowchart LR
    subgraph 输入
        S[状态 s]
        A[动作 a]
        R[奖励 r]
        S_next[下一状态 s_next]
    end

    subgraph 目标值计算
        A_next[动作 a_next]
        Q_target[Q_theta_bar]
        LogPi[log pi_phi]
        V[V 值]
        Target[目标值]
    end

    subgraph 损失
        Q_current[Q_theta]
        MSE[MSE Loss J_Q]
    end

    S_next --> A_next
    A_next --> Q_target
    A_next --> LogPi
    Q_target --> V
    LogPi --> V
    V --> Target
    R --> Target
    S --> Q_current
    A --> Q_current
    Q_current --> MSE
    Target --> MSE
```

## 4. Soft 策略改进（Actor 更新）

```mermaid
flowchart LR
    subgraph 输入
        S[状态 s]
        Noise[噪声 epsilon]
    end

    subgraph 重参数化
        F[重参数化函数 f_phi]
        A[动作 a]
    end

    subgraph 目标
        Q_min[最小 Q 值]
        LogP[log pi_phi]
        Alpha[alpha]
        Obj[Actor Loss J_pi]
    end

    S --> F
    Noise --> F
    F --> A
    A --> Q_min
    A --> LogP
    LogP --> Obj
    Q_min --> Obj
    Alpha --> Obj
```

## 5. 自动温度调节

```mermaid
flowchart LR
    subgraph 约束优化问题
        Obj[最大化期望奖励]
        Constraint[约束 期望熵大于等于 H_bar]
    end

    subgraph 对偶问题
        Dual[最小化 J_alpha]
        Alpha[温度参数 alpha]
    end

    subgraph 梯度更新
        Grad[计算梯度]
        Update[更新 alpha]
    end

    Obj --> Constraint
    Constraint --> Dual
    Dual --> Alpha
    Alpha --> Grad
    Grad --> Update
    Update -->|新 alpha| Alpha
```

## 6. 双 Q 函数与目标网络

```mermaid
flowchart TD
    subgraph 经验回放
        RB[Replay Buffer]
    end

    subgraph 当前Q网络
        Q1[Q1 theta1]
        Q2[Q2 theta2]
    end

    subgraph 目标网络
        T1[Target Q1]
        T2[Target Q2]
    end

    subgraph 计算
        Min[取最小值]
        V[V 值计算]
        Target[目标值]
        Loss[J_Q 损失]
    end

    subgraph 策略
        Actor[Actor pi]
    end

    RB -->|s a r s_next| Q1
    RB -->|s a r s_next| Q2
    RB -->|s| Actor

    Actor -->|a_next| T1
    Actor -->|a_next| T2

    T1 --> Min
    T2 --> Min
    Min --> V
    V --> Target
    R --> Target

    Q1 --> Loss
    Q2 --> Loss
    Target --> Loss

    Q1 -.->|软更新 tau| T1
    Q2 -.->|软更新 tau| T2
```

## 7. 关键公式汇总

$$
\pi^* = \arg\max_\pi \sum_t \mathbb{E}[r(s_t,a_t) + \alpha\mathcal{H}(\pi(\cdot|s_t))]
$$
> **最大熵目标函数**：SAC 的核心目标，在最大化累积奖励的同时最大化策略熵

$$
T^\pi Q(s_t,a_t) = r(s_t,a_t) + \gamma\mathbb{E}_{s_{t+1}\sim p}[V(s_{t+1})]
$$
> **Soft Bellman 备份算子**：用于 Soft 策略评估的迭代算子

$$
V(s_t) = \mathbb{E}_{a_t\sim\pi}[Q(s_t,a_t) - \alpha\log\pi(a_t|s_t)]
$$
> **Soft 状态值函数**：将熵项纳入状态值估计

$$
\pi_{\text{new}} = \arg\min_{\pi'\in\Pi} D_{\text{KL}}\left(\pi'(\cdot|s_t) \Big\| \frac{\exp(\frac{1}{\alpha}Q^{\pi_{\text{old}}}(s_t,\cdot))}{Z^{\pi_{\text{old}}}(s_t)}\right)
$$
> **Soft 策略改进**：通过最小化 KL 散度更新策略

$$
J_Q(\theta) = \mathbb{E}_{(s_t,a_t)\sim\mathcal{D}}\left[\frac{1}{2}\left(Q_\theta(s_t,a_t) - (r + \gamma V_{\bar{\theta}}(s_{t+1}))\right)^2\right]
$$
> **Critic 损失函数**：最小化 Soft Bellman 残差

$$
J_\pi(\phi) = \mathbb{E}_{s_t\sim\mathcal{D},\epsilon_t\sim\mathcal{N}}[\alpha\log\pi_\phi(f_\phi(\epsilon_t;s_t)|s_t) - Q_\theta(s_t,f_\phi(\epsilon_t;s_t))]
$$
> **Actor 损失函数（重参数化）**：使用重参数化技巧的梯度估计

$$
J(\alpha) = \mathbb{E}_{a_t\sim\pi_t}[-\alpha\log\pi_t(a_t|s_t) - \alpha\bar{\mathcal{H}}]
$$
> **温度参数损失函数**：自动调节熵权重的对偶目标

---

*基于 Haarnoja et al. (2019) "Soft Actor-Critic Algorithms and Applications"*

---

Written by LLM-for-Zotero.
