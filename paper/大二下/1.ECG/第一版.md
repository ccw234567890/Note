现在该做 **Day 2：baseline 跑通**。

目标很简单：先训练一个最小 ECG 分类器，确认模型能正常学习 PTB-XL。

你现在已经完成了：

```text
数据下载
数据完整性检查
Dataset/DataLoader
batch shape = [B, 12, 1000]
label shape = [B, 5]
```

下一步应该做：

```text
ECG [B, 12, 1000]
→ 1D CNN Encoder
→ Global Average Pooling
→ Linear Classifier
→ 5-label sigmoid output
```

loss 只用：

```text
BCEWithLogitsLoss
```

指标先做：

```text
AUROC
AUPRC
F1
train loss
val loss
```

建议我们现在先写这些文件：

```text
ecg_koopman/models/encoder.py
ecg_koopman/models/baseline.py
ecg_koopman/train/train_baseline.py
```

第一版不要追求 SOTA，只要能稳定跑完，得到一个合理 clean validation/test AUROC。之后才加人工噪声测试。